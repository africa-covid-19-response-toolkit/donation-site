{
  "name": "stripe-recipe-demo",
  "version": "1.0.0",
  "description": "A Stripe demo",
  "main": "index.js",
  "scripts": {
    "start": "export IS_OFFLINE=true && serverless offline start -s dev --noStart",
    "start:dynamodb": "sls dynamodb start",
    "dev": "concurrently \"yarn start:dynamodb\" \"nodemon --exec yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "sls dynamodb install"
  },
  "engines": {
    "node": "^10.13.0"
  },
  "author": "stripe-demos",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "joi": "^14.3.1",
    "serverless-s3bucket-sync": "^0.1.5",
    "stripe": "^7.1.0",
    "uuid": "^8.0.0"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4",
    "serverless": "^1.67.0",
    "serverless-dynamodb-local": "0.2.30",
    "serverless-offline": "^6.1.5",
    "serverless-s3-sync": "^1.13.0",
    "serverless-single-page-app-plugin": "^1.0.2"
  }
}
